# 面试录 - 开发步骤规划

## 开发原则

### 稳定推进
- **小步快跑**：每次只开发一个功能模块
- **及时验证**：每个步骤完成后进行测试
- **保持整洁**：随时重构，保持代码质量

### 安全开发
- **版本控制**：每个功能完成前提交 Git
- **备份数据**：重要修改前备份模型
- **渐进式开发**：先实现核心功能，再优化体验

### 有效迭代
- **用户反馈**：每完成一个功能后确认
- **问题优先**：发现问题立即修复
- **文档同步**：代码和文档同步更新

---

## 开发阶段

### 🎯 Phase 1：数据层（已完成）

#### Step 1.1：数据模型设计 ✅
**目标**：定义清晰的数据结构

**任务清单**：
- [x] 创建 `Models.swift` 文件
- [x] 定义 `JobApplication` 模型
- [x] 定义 `InterviewRound` 模型
- [x] 定义 `ApplicationStatus` 枚举
- [x] 定义 `InterviewResult` 枚举
- [x] 添加预览数据

**验证标准**：
- 模型编译通过
- 关系定义正确
- 预览数据可用

**完成时间**：2026-05-31

---

### 🎯 Phase 2：首页（Dashboard）

#### Step 2.1：ContentView 基础框架
**目标**：创建首页列表展示

**任务清单**：
- [ ] 创建 `ContentView.swift`
- [ ] 实现 `List` 列表展示
- [ ] 实现求职记录行视图
- [ ] 添加导航标题
- [ ] 添加"+"按钮

**代码示例**：
```swift
struct ContentView: View {
    @Query private var applications: [JobApplication]
    
    var body: some View {
        NavigationStack {
            List(applications) { app in
                ApplicationRow(application: app)
            }
            .navigationTitle("求职记录")
            .toolbar {
                Button(action: addApplication) {
                    Image(systemName: "plus")
                }
            }
        }
    }
}
```

**验证标准**：
- 列表正常显示
- 数据正确加载
- 按钮可点击

---

#### Step 2.2：求职记录行视图
**目标**：设计美观的列表项

**任务清单**：
- [ ] 创建 `ApplicationRow` 视图
- [ ] 显示公司名（加粗）
- [ ] 显示岗位名
- [ ] 显示状态标签（颜色区分）
- [ ] 添加点击进入详情

**设计规范**：
- 公司名：`.headline` 字体
- 岗位名：`.subheadline` 字体
- 状态标签：圆角胶囊样式
- 间距：8pt 内边距

**验证标准**：
- 布局美观
- 颜色正确
- 点击响应

---

#### Step 2.3：侧滑删除功能
**目标**：实现列表项删除

**任务清单**：
- [ ] 添加 `.onDelete` 修饰符
- [ ] 实现删除确认对话框
- [ ] 处理 SwiftData 删除操作
- [ ] 添加删除动画

**代码示例**：
```swift
List {
    ForEach(applications) { app in
        ApplicationRow(application: app)
    }
    .onDelete { indexSet in
        deleteApplications(at: indexSet)
    }
}
```

**验证标准**：
- 侧滑显示删除按钮
- 点击删除有确认
- 删除后列表更新

---

### 🎯 Phase 3：添加求职记录

#### Step 3.1：AddApplicationView 表单
**目标**：创建添加求职记录的表单

**任务清单**：
- [ ] 创建 `AddApplicationView.swift`
- [ ] 实现表单输入
- [ ] 添加表单验证
- [ ] 实现保存功能
- [ ] 添加取消按钮

**表单字段**：
| 字段 | 类型 | 必填 | 验证 |
|------|------|------|------|
| 公司名称 | TextField | ✅ | 非空 |
| 岗位名称 | TextField | ✅ | 非空 |
| 投递日期 | DatePicker | ✅ | 默认今天 |
| 工作地点 | TextField | ❌ | - |
| 薪资范围 | TextField | ❌ | - |
| 备注 | TextEditor | ❌ | - |

**验证标准**：
- 表单正常显示
- 验证生效
- 保存成功

---

#### Step 3.2：表单验证逻辑
**目标**：确保数据有效性

**任务清单**：
- [ ] 实现必填字段验证
- [ ] 显示错误提示
- [ ] 禁用无效提交
- [ ] 添加实时验证

**验证规则**：
- 公司名称：不能为空
- 岗位名称：不能为空
- 投递日期：不能晚于今天

**验证标准**：
- 空值无法提交
- 错误提示清晰
- 提交按钮状态正确

---

### 🎯 Phase 4：详情页

#### Step 4.1：DetailView 基础框架
**目标**：展示求职记录详情

**任务清单**：
- [ ] 创建 `DetailView.swift`
- [ ] 显示公司和岗位信息
- [ ] 显示基本信息卡片
- [ ] 添加导航标题
- [ ] 添加编辑按钮

**布局设计**：
- 顶部：公司名和岗位名
- 中间：基本信息（状态、日期、地点等）
- 底部：面试轮次列表

**验证标准**：
- 信息显示正确
- 导航正常
- 编辑按钮可用

---

#### Step 4.2：面试轮次列表
**目标**：展示面试轮次记录

**任务清单**：
- [ ] 创建 `InterviewRoundList` 视图
- [ ] 显示轮次名称
- [ ] 显示面试日期
- [ ] 显示面试结果
- [ ] 支持删除轮次

**设计规范**：
- 使用时间轴样式
- 结果用颜色标识
- 支持滑动删除

**验证标准**：
- 列表正常显示
- 数据正确关联
- 删除功能正常

---

#### Step 4.3：添加面试轮次
**目标**：实现添加面试轮次功能

**任务清单**：
- [ ] 创建 `AddInterviewRoundView.swift`
- [ ] 实现表单输入
- [ ] 支持自定义轮次名称
- [ ] 实现保存功能
- [ ] 关联到求职记录

**表单字段**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 轮次名称 | TextField | ✅ | 支持自定义 |
| 面试日期 | DatePicker | ✅ | - |
| 面试官 | TextField | ❌ | - |
| 本轮结果 | Picker | ✅ | 4种选项 |
| 面试反馈 | TextEditor | ❌ | - |

**验证标准**：
- 表单正常显示
- 保存成功
- 关联正确

---

### 🎯 Phase 5：交互优化

#### Step 5.1：状态颜色系统
**目标**：实现动态状态颜色

**任务清单**：
- [ ] 实现状态颜色映射
- [ ] 添加状态标签组件
- [ ] 实现颜色动画
- [ ] 支持深色模式

**颜色定义**：
| 状态 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 已投递 | 蓝色 | 蓝色 |
| 笔试 | 紫色 | 紫色 |
| 面试中 | 橙色 | 橙色 |
| Offer | 绿色 | 绿色 |
| 已拒绝 | 灰色 | 灰色 |
| 未通过 | 红色 | 红色 |

**验证标准**：
- 颜色显示正确
- 深色模式适配
- 动画流畅

---

#### Step 5.2：动画效果
**目标**：提升用户体验

**任务清单**：
- [ ] 添加页面切换动画
- [ ] 添加列表项动画
- [ ] 添加状态变更动画
- [ ] 添加按钮点击反馈

**动画类型**：
- 页面切换：`.slide` 或 `.opacity`
- 列表删除：`.offset` + `.opacity`
- 状态变更：`.scale` + `.opacity`
- 按钮点击：`.spring()`

**验证标准**：
- 动画流畅自然
- 不影响性能
- 增强体验

---

### 🎯 Phase 6：完善收尾

#### Step 6.1：深色模式支持
**目标**：完整支持深色模式

**任务清单**：
- [ ] 测试所有页面
- [ ] 调整颜色适配
- [ ] 测试对比度
- [ ] 修复问题

**验证标准**：
- 所有页面正常显示
- 对比度符合标准
- 无颜色问题

---

#### Step 6.2：性能优化
**目标**：确保流畅运行

**任务清单**：
- [ ] 优化列表性能
- [ ] 减少不必要的重绘
- [ ] 优化数据加载
- [ ] 测试内存使用

**验证标准**：
- 列表滚动流畅
- 无卡顿现象
- 内存使用正常

---

#### Step 6.3：测试和修复
**目标**：确保质量

**任务清单**：
- [ ] 功能测试
- [ ] 边界测试
- [ ] 异常测试
- [ ] 修复问题

**验证标准**：
- 所有功能正常
- 无崩溃现象
- 体验良好

---

## 开发检查清单

### 每个步骤完成后
- [ ] 代码编译通过
- [ ] 功能正常运行
- [ ] 无警告和错误
- [ ] 提交 Git
- [ ] 更新开发日志

### 每个阶段完成后
- [ ] 功能完整可用
- [ ] 代码质量良好
- [ ] 文档更新完成
- [ ] 演示给用户确认

### 项目完成前
- [ ] 所有功能实现
- [ ] 性能优化完成
- [ ] 深色模式支持
- [ ] 测试通过
- [ ] 文档完整

---

## 风险控制

### 技术风险
- **风险**：SwiftData 兼容性问题
- **应对**：及时查阅文档，保持代码简洁

### 进度风险
- **风险**：功能复杂度超预期
- **应对**：优先核心功能，非核心功能可延后

### 质量风险
- **风险**：代码质量下降
- **应对**：及时重构，保持代码整洁

---

## 沟通机制

### 日常沟通
- 每完成一个步骤，向用户汇报
- 遇到问题及时沟通
- 重要决策前征求用户意见

### 进度同步
- 每天更新开发日志
- 每周总结项目进度
- 及时调整开发计划
